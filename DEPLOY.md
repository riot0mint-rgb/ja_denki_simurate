# デプロイメント・運用ガイド (DEPLOY.md)

**状態**: フェーズ1 - 骨子作成中

本ドキュメントは、JAでんき料金比較シミュレータの導入・運用・更新手順をボタン操作レベルで説明します。

---

## Phase A デプロイメント（静的Web App）

### 前提条件

- Node.js 18+ がインストール済み
- npm または yarn が利用可能
- HTTPS対応のホスティング環境（SharePoint / 社内Webサーバー / Azure Static Web Apps）

### セクション構成（TBD フェーズ3-5で記載）

1. **ローカル開発環境のセットアップ**
   - リポジトリクローン
   - 依存パッケージインストール
   - ビルド確認
   - ローカルサーバー起動

2. **ビルドと本番成果物作成**
   - `npm run build` 実行
   - 出力ファイル確認（dist/ フォルダ）
   - PWA マニフェスト確認

3. **デプロイメント先別手順**

   **3-1. SharePoint へのアップロード**
   - 対象フォルダの特定
   - dist/ 内のファイルをまとめてアップロード
   - インデックス設定（index.html をデフォルトに）
   - アクセス確認（URL）

   **3-2. 社内Webサーバー（IIS等）へのデプロイ**
   - 管理者権限で接続
   - dist/ フォルダを配置
   - MIME タイプ設定（.js, .wasm等）
   - リバースプロキシ / HTTPS設定

   **3-3. Azure Static Web Apps へのデプロイ**
   - リソース作成（Azure Portal）
   - GitHub連携設定
   - 自動ビルド・デプロイ設定
   - カスタムドメイン設定（オプション）

4. **PWAとしてのスマートフォン追加**
   - ホーム画面への追加手順（iOS / Android別）
   - オフライン動作確認
   - キャッシュ更新ポリシー

5. **料金マスター更新手順**

   **5-1. JSON差し替えによる更新（Phase A）**
   - 更新ファイル: `data/rate_master.json` ほか
   - バックアップ作成（前版の自動保存確認）
   - 新版ファイルを配置
   - 再ビルド: `npm run build`
   - テスト環境で回帰テスト実施
   - 本番デプロイ

   **5-2. 管理画面からの更新（Phase B）**
   - 管理者アカウント（AAD）でログイン
   - 「料金マスター管理」→「新規版追加」
   - 更新項目を入力
   - 代表使用量（300kWh, 900kWh）で自動検算
   - 承認ボタン押下
   - 自動デプロイ実行

6. **トラブルシューティング**
   - よくあるエラーと対応
   - ログ確認方法
   - キャッシュクリア手順

7. **ロールバック（切り戻し）**
   - 前版の復旧手順
   - git revert コマンド例
   - バージョン管理の確認方法

---

## Phase B デプロイメント（Azure Functions + OCR）

### セクション構成（TBD フェーズ7-9で記載）

1. Azure Functions の設定
2. Azure AI Document Intelligence（OCR）の設定
3. 料金自動取得ボットのスケジュール設定
4. AI案内役の API 統合

---

## トレーニング・運用マニュアル

### 対象者別手順

**営業担当者向け**:
- シミュレーターの起動方法
- 基本的な操作フロー
- よくあるご質問への回答

**システム管理者向け**:
- 月次更新（燃料費調整・再エネ賦課金）
- 料金改定時の対応
- エラーログの確認

**開発チーム向け**:
- calc-core の更新手順
- テストの実行方法
- CI/CD パイプラインの確認

---

## セキュリティ・チェックリスト

デプロイ前に以下を確認：

- [ ] HTTPS のみ（HTTPリダイレクト有効）
- [ ] CSP（Content Security Policy）ヘッダー設定
- [ ] localStorage に個人情報なし
- [ ] API キー等が環境変数（.env）に外出しなし
- [ ] コンソールから個人情報出力なし（console.log削除）

---

## 自動テスト・回帰テスト

各デプロイ前に自動実行：

```bash
npm run test:unit      # ユニットテスト（calc-core）
npm run test:regression # 回帰テスト（既存Excel比較）
npm run test:security   # セキュリティスキャン
```

---

## 監査・コンプライアンス

記録すべき項目：

- デプロイ日時、実行者
- 料金マスター変更履歴（JSON差分）
- エラー発生時のアクション
- ユーザーからの問い合わせ

---

**最終更新**: 2026-08-20（骨子）
**詳細化予定**: フェーズ3-5
**関連文書**: SCRAPING_POLICY.md, SECURITY.md, CLAUDE.md
