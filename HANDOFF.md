# 引き継ぎ書 (HANDOFF.md)

**最終更新**: 2026-08-20 / ブランチ `claude/ja-denki-comparison-simulator-mewi5f` / PR #2
（ナイトホリデー解決・公式出典の裏付けは `claude/night-holiday-rates` で追加）

セッションが切り替わった場合、まずこのファイルと `ASSUMPTIONS.md` を読むこと。
`CLAUDE.md` の非交渉ルール10項目は常に優先される。

---

## 1. 現在地

JAでんき料金比較シミュレータ（Web版）。**11シナリオの「JAでんき VS 他社」比較が実装済み**で、
公式試算表 Excel の計算式・単価をすべて写し取り、回帰テストで固定してある。

- テスト **189件全通過 / calc-core カバレッジ 100%**
- CI グリーン、PR #2（draft・mergeable）
- 最新コミット: `test: 営業資料に明記された中国電力との差額をテストで固定`

## 2. 次にやること（最優先）

**電気需給約款（低圧家庭用）を入手し、料金全体の端数処理規定を確定する。**

従量電灯S の52点1円差（下記4）を決着させるにはこれが要る。JAでんき料金メニュー
定義書を確認したところ、端数処理の規定は**再エネ賦課金についてしか書かれていない**。

> 再生可能エネルギー発電促進賦課金の計算における合計金額の単位は１円とし、その端数は切り捨てます。

料金全体の端数処理は定義書ではなく需給約款の側にあり、定義書は各所からそれを参照している。
全農エネルギーに定義書と一緒に取り寄せるのが確実。

### 済: 各社の公式料金資料の取得（2026-08-20）

ネットワークが通るセッションで下記を取得し、**全プランの単価が一致することを確認済み**。
突合は `packages/calc-core/tests/officialSources.test.ts` に固定してある。

| 資料 | 対象プラン |
|---|---|
| 単価表（電灯） `/pricelist1.html` | 従量電灯A・従量電灯B |
| 単価表（電灯・選択約款） `/pricelist2.html` | ファミリータイムⅠ／Ⅱ・エコノミーナイト |
| 単価表（電力） `/pricelist3.html` | 低圧電力 |
| 単価表（電力・選択約款） `/pricelist4.html` | 深夜電力B |
| 単価表（電灯・電力 サービス約款） `/pricelist5.html` | スマート・シンプル・電化Style・ナイトホリデー |
| 燃料費等調整制度のご案内 `/elec/seido/nencho/` | 燃調（規制・自由の2系統） |
| JAでんき 料金メニュー定義書 2024年4月改定 | JAでんき 従量電灯A/S/B・低圧電力・夜トク |

いずれも `www.energia.co.jp` 配下（JAでんき定義書は `hirokumi.net`）。
`rates.ts` の `sources` は試算表の出典に加えて上記を併記した。
**全 11 プランが一致**し、低圧電力の食い違いも解消した（規制料金「低圧電力」1,163.92円/kW が
比較対象で、自由料金「動力コース」1,152.44円/kW は別メニューだった）。

**JAでんき定義書は 2024年4月改定版が公開されている最新で、2025/2026年の改定有無は未確認。**
2026年8月版の推進資料の値引き幅（第2段階 ▲1.39／第3段階 ▲2.71／従量電灯S 最低料金 ▲89.76／
夜トク ▲121.0）とは整合するので現行値と見ているが、照会が要る。

### ネットワークの状態（重要）

ユーザーがクラウド環境「Default」(`env_01G4Zs49928j6BCd1nC8eoH2`) の
Network access を変更済み（Trusted → Custom/Full）。
**ただしポリシーはVM起動時に焼き込まれるため、変更前に起動したセッションでは反映されない。**

新しいVMで起動したセッションなら以下が取得できるはず。まず疎通確認すること:

```
WebFetch https://www.energia.co.jp/elec/h_menu/pricelist/pricelist1.html
```

遮断されるようなら、ユーザーに「環境設定は変わっているが、このセッションのVMが
古いポリシーのまま。セッションをアーカイブ→アーカイブ解除→メッセージ送信でVMが
作り直され、会話履歴も復元される」と伝える。

### 取得したい資料

| 事業者 | 資料 | 想定URL |
|---|---|---|
| 中国電力 | 電気料金単価表（電灯） | www.energia.co.jp/elec/h_menu/pricelist/pricelist1.html |
| 中国電力 | 電気料金単価表（電灯・選択約款） | 同 /pricelist2.html |
| 中国電力 | 電気料金単価表（電灯・電力 サービス約款） | 同 /pricelist5.html |
| 中国電力 | 電気料金の早見表・単価表（PDF一覧） | www.energia.co.jp/elec/pricelist.html |
| auでんき | 料金プラン | www.au.com/electricity/ |
| JAでんき | 料金メニュー定義書 | www.ja-denki.jp / 全農エネルギー |

**代替手段**: Google Drive は問題なく読める。ユーザーにPDFをDriveへ置いてもらえば
ネットワークなしで進められる（試算表もこの方法で入手した）。

### 資料が取れたらやること

1. 実装済み11プランの単価を1件ずつ照合（`apps/web/src/data/rates.ts`）
2. **ナイトホリデーの基本料金**を確定 → `unsupported` を解除（下記4を参照）
3. 約款の端数処理規定を確認 → **従量電灯Sの1円差**の決着（下記4を参照）
4. 消費税の課税箇所を確認
5. `RateSource` の `document` / `locator` を試算表から公式定義書に差し替え

---

## 3. 実装済みの内容

### 対応シナリオ（11件）

| 現在のご契約 | 乗り換え先 | 計算構造 | 元資料 |
|---|---|---|---|
| 中国電力 従量電灯A | JA従量電灯A / S | tiered_minimum | ① |
| 中国電力 スマートコース | JA従量電灯A / S | tiered_minimum | ① |
| 中国電力 シンプルコース | JA従量電灯A / S | flat_rate | ① |
| 中国電力 従量電灯B | JA従量電灯B | capacity_tiered | ② |
| 中国電力 低圧電力 | JA低圧電力 | demand_seasonal | ⑤ |
| 中国電力 電化Style | JA夜トクプラン | time_of_use | ③ |
| 中国電力 ナイトホリデー | JA夜トクプラン | time_of_use（**計算不可**） | ③ |
| 中国電力 深夜電力B | JA夜トクプラン | demand_flat | ⑥ |
| 中国電力 ファミリータイムⅠ | JA夜トクプラン | family_time | ④ |
| 中国電力 ファミリータイムⅡ | JA夜トクプラン | family_time | ④ |
| 中国電力 時間帯別電灯 | JA夜トクプラン | economy_night | ④ |
| auでんき Mプラン | JA従量電灯A / S | tiered_minimum（切り上げ丸め） | au |

### 構成

```
packages/calc-core/src/
  models.ts            プラン型（6構造）・UsageInput・MonthlyBill
  calculator.ts        全構造の計算。元資料のセル番地をコメントに明記
  comparator.ts        比較・削減額・セット割・初年度割引
  monthlyRates.ts      燃調・賦課金の年×月テーブル（2025-01〜2026-07）
  touAllocation.ts     ファミリー/時間帯別 → 夜トク の時間帯振替（推定を含む）
  japaneseHolidays.ts  検針期間から日数・土日・祝日を算出
  rounding.ts          円未満の切り捨て/切り上げ（Excel ROUNDDOWN/ROUNDUP 準拠）
apps/web/src/
  data/rates.ts        全プランの単価＋出典＋シナリオ定義
  services/calculateService.ts  シナリオ解決・使用量の振替・表示用整形
  pages/ManualInput.tsx         入力フォーム（構造ごとに切替）
  pages/ComparisonResult.tsx    結果表示
```

### 設計上の判断（変更する前に理由を確認すること）

- **推奨プランは定数で持たない**。217kWh分岐は営業資料の記載どおりだが、
  単価改定で動くため実際の算出額を比較して安い方を選ぶ。
- **燃調・賦課金は月次テーブル**。1か月分を定数で持つと改定のたびに乖離する。
  未収録の月は `null` を返し推測しない。
- **最低料金は「最初の15kWhを含む定額」**。基本料金の代替ではない。
  従量料金は15kWh超から始まる（過去に二重計上のバグがあった）。
- **端数は円未満切り捨て**（四捨五入ではない）。賦課金を先に切り捨ててから
  合計し、合計でもう一度切り捨てる二段構成。auでんきのみ従量料金と燃調を切り上げ。
- **入力検証は必ず通す**。NaN・負値は `unsupported` を返す（ルール8）。

---

## 4. 未解決・確認待ち

### ✅ 解決: ナイトホリデーの基本料金（2026-08-20）

**ナイトホリデーコースに基本料金は存在しなかった。** 中国電力の公式単価表で、
このコースだけシンプルコースと同じ**最低月額料金型**（1,844.70円/契約）と定義されている。

③の J8:J10 が空欄なのは記載漏れではなく、**そこに入る金額が無いため**だった。
0 として計算せず `unsupported` にした判断は正しかった。

| コース | 体系 |
|---|---|
| 電化Style | 基本料金 2,018.72円/契約（10kWまで）+ 480.37円/kW（10kW超） |
| ナイトホリデー | 最低月額料金 1,844.70円/契約のみ（契約電力に依らない） |

`TimeOfUsePlan` に `minimumMonthly` を足し、基本料金型と最低月額料金型の
どちらでも計算できるようにした。`unsupported` は**基本料金も最低月額料金も無い**
場合だけ返す。

### ⚠ 従量電灯S の52点で Excel と1円差

0〜1200kWh の1201点中52点で本実装が Excel より1円高い。
原因は **Excel側のIEEE754浮動小数点誤差**（厳密な合計がちょうど整数になるケース）。
CLAUDE.md ルール2に従い Decimal の厳密値を採用。該当kWhは
`tests/excelReference.test.ts` に定数列挙して監視している。
**請求実務でどちらが正かの確認待ち。** 実務がExcel側なら
「合計が整数のとき1円引く」という規則として明示実装する必要がある。

補足（2026-08-20）: 別の Excel（かんたんシミュレーションの早見表）でも同種の
1円差を踏んだが、**ずれの向きが逆**だった（Excel が厳密値どおりで、素朴な
切り捨てのほうが1円低い）。そちらは「有効数字15桁に正規化してから切り捨て」で
全点一致したが、**この規則をここに持ち込むと52点は逆に開く。**
ファイルごとに数式構造が違うため、Excelの挙動から普遍則を導くのは筋が悪い。
**需給約款の端数処理規定で決めるのが正道**（上記2）。

### ⚠ 時間帯別電灯（エコノミーナイト）に金額の突合点がない

④の入力が全て0のため、構造と単価しか検証できていない。
**実使用量が入った試算例を1件もらえれば回帰テストに固定できる。**

### ⚠ auでんきMプランは7月時点でJAでんきより安い

燃調がau -13.09円/kWh に対し中国エリア -9.57円/kWh と差が大きく、
348kWhで au 10,185円 / JA 10,977円。元資料のau明細（0kWhでau625円/JA678円）とも
方向が一致し計算誤りではない。**営業上そのまま表示してよいかの判断待ち。**

### ⚠ ④ファミリー/時間帯別の夜トク換算は推定

「ホリデータイム」の区分がないため、検針期間の平日／休日日数比と
「休日電力使用割合」（3択・主観）から按分している。元資料の式どおりだが実測ではない。

### 元資料どうしの矛盾（テストで固定済み）

| 項目 | 専用シート | 別シート | 採用 |
|---|---|---|---|
| 夜トクの基本料金半額（使用量0） | ③：適用 | ⑥：非適用 | ③ |
| JA従量電灯A 最低料金 | ①：759.68 | au版：759.67 | ① |
| JA側 燃調小計の丸め | ①：なし | au版：ROUNDDOWN | ① |

「ファミリーⅠ/Ⅱ結果」「時間帯別結果」の**従量電灯Aとの比較列は `#REF!` で壊れている**
ため未実装（夜トク・電化Styleとの比較のみ）。

---

## 5. 検証の要点

回帰テストは「実装の出力」ではなく**元資料の値**を期待値にしている。これを崩さないこと。

| テスト | 内容 |
|---|---|
| `excelReference.test.ts` | ①早見表 0〜1200kWh 全点 + 各明細シートの基準ケース |
| `familyTime.test.ts` | ④ファミリーⅡ実データ（15,876 / 16,180 / 16,301円・差額+304円） |
| `salesDeckCrossCheck.test.ts` | 営業資料の差額（-1.39 / -2.71 / -89.76 / -31.09 / -121.0 等） |
| `japaneseHolidays.test.ts` | 祝日算出（ハッピーマンデー・春分秋分・年跨ぎ・土日重複） |
| `calculator.test.ts` | 全6構造の境界値と unsupported 分岐 |
| `comparator.test.ts` | 推奨プラン選択（0〜1200kWh全点で最安と一致）・割引 |

コマンド:

```bash
npm run test:coverage   # 152件・100%（閾値未達なら失敗する）
npm run build
npm run type-check
```

元資料の xlsx は `archive/` にあるが **.gitignore 済み**（正本は Google Drive）。
VMが作り直されると消えるので、必要なら Drive から再取得する
（`シミュレーション【26年7月適用】/中電 26年7月/` ほか）。

## 6. 元資料の場所（Google Drive）

| 番号 | ファイル |
|---|---|
| ① | JAでんき試算表(VS中電_従量A・スマート・シンプル)26年7月適用.xlsx |
| ② | JAでんき試算表(VS中電_従量B)26年7月適用.xlsx |
| ③ | JAでんき試算表(VS中電_電化Style・ナイトホリデー)26年7月適用.xlsx |
| ④ | JAでんき試算表(VS中電_ファミリー①②・時間帯別)26年7月適用.xlsx |
| ⑤ | JAでんき試算表(VS中電_低圧電力)26年7月適用.xlsx |
| ⑥ | JAでんき試算表(VS深夜電力B)26年7月適用.xlsx |
| au | JAでんき試算表(VS auでんき_Ｍプラン / 低圧電力)26年6月.xlsx |

営業資料: `広島市 JAでんき案内資料 r3.pptx`、`JAでんき_職員向け説明会.pptx`、`JAでんき_QA_統合版.docx`

**元資料は読み取り専用**（CLAUDE.md ルール3）。Drive上で変更・移動・削除しないこと。
