# セキュリティ・個人情報保護方針 (SECURITY.md)

## Phase A（初期版）のセキュリティ設計

### 原則

このプロジェクトは家庭向け電気料金比較シミュレータであり、**検針票**など個人情報を含む可能性のあるドキュメントを扱います。

**最優先原則**: Phase A では個人情報をサーバーに送信しない、ブラウザ内で完結する

### 個人情報の定義と取扱

#### 本システムが取り扱う「個人情報的なデータ」

| データ | 状況 | 保存場所 | 保持期間 | 備考 |
|--------|------|---------|----------|------|
| 電気使用量（kWh） | 必須入力 | ブラウザメモリのみ | セッション終了時削除 | 数値のみ |
| 契約会社名 | 必須入力 | ブラウザメモリのみ | セッション終了時削除 | 選択肢から選択 |
| 契約アンペア数 | オプション入力 | ブラウザメモリのみ | セッション終了時削除 | 数値のみ |
| 計算結果（月額・年額差分） | 一時的 | ブラウザメモリ | セッション終了時削除 | 個人属性なし |
| 検針票画像 | 一時的※1 | ブラウザメモリのみ※1 | セッション終了時削除※1 | Phase B |

**※1 Phase B（OCR機能）**: 画像はクライアント側で一時保持、Azure AI Document Intelligenceへ送信時はAzure環境内での処理のみ、生画像の記録なし

#### 本システムが取り扱わない情報（排除）

- 氏名、住所、電話番号
- 契約者ID、顧客番号
- 口座番号、支払方法
- メールアドレス
- 納期日、支払期限
- 使用量の時間帯別内訳（スマートメーター詳細）

### データフロー設計

```
Phase A (静的Web App)
┌─────────────────┐
│  ブラウザ       │
├─────────────────┤
│ 入力値          │ ← ユーザー入力（使用量、会社名など）
│ 料金マスター    │ ← バンドル済みJSON
│ 計算エンジン    │ ← TypeScript/Decimal.js
│ 計算結果        │ ← メモリのみ
│ PDF生成         │ ← jsPDF/pdf-make（ブラウザ側）
└─────────────────┘
        ↓ （入力値・画像の送信なし）
   サーバー （なし）
```

```
Phase B (Azure環境、任意)
┌─────────────────┐           ┌──────────────────┐
│  ブラウザ       │           │ Azure Functions  │
├─────────────────┤           ├──────────────────┤
│ 入力値          │───────→   │ 料金自動取得     │
│ 料金マスター    │ (呼び出し) │ (Python)         │
│ 計算エンジン    │←───────   │ スクレイピング   │
│ 計算結果        │           │ 値の検証         │
└─────────────────┘           └──────────────────┘
                                      ↓
                                 ┌──────────────────┐
                                 │ Azure Static     │
                                 │ Web Apps         │
                                 │ (検針票OCR)      │
                                 └──────────────────┘
```

### 個人情報保護のための実装要件

#### Phase A

**✓ 必須実装**

1. **データ永続化の禁止**
   - localStorage, sessionStorage に個人情報的データを保存しない
   - デフォルト状態では任意の保存機能がない
   - ユーザーが明示的に「計算結果を保存」を選択した場合のみ一時保存
   - 保存データは「試算ID + 計算結果（金額のみ）」
   - 個人属性（契約会社、使用量）は保存しない

2. **ローカルストレージ安全設計**
   - 平文保存なし、必要に応じてbase64エンコード程度
   - 保存期間明示（例: 7日で自動削除、手動削除機能付き）
   - インデックスなし（ブラウザの自動補完対象外）

3. **ネットワークセキュリティ**
   - HTTP通信なし、HTTPS only（静的ホスティング時）
   - サーバーへのAJAX送信：個人情報的データは絶対に送らない
   - 料金マスター更新：差分JSONのみ、住環境課内デプロイ

4. **ブラウザAPIの安全使用**
   - `navigator.clipboard`: 結果コピー時はテキストのみ
   - `File API`: Phase B OCR用、ブラウザ内処理後は即破棄
   - `IndexedDB`: 使用しない（データベース機能不要）

5. **XSS・インジェクション対策**
   - 計算結果表示: React/Vue/Svelte等のテンプレートエンジンで自動エスケープ
   - ユーザー入力: 数値・選択肢のみ受け入れ、テキスト入力なし
   - PDF生成: ユーザー入力は「計算根拠」として読み取り専用表示

6. **暗号化・ハッシング**
   - Phase A: 不要（ブラウザ内のみ）
   - Phase B: 料金マスター自動取得時にPDFハッシュ値記録（改ざん検知）

#### Phase B (オプション、将来実装)

**✓ Azure環境内での実装原則**

1. **Azure AI Document Intelligence (OCR)**
   - 検針票画像はAzure Cognitiveサービスへ送信
   - 送信元: 静的Web Apps → Azure Functions → Document Intelligence
   - 結果: 抽出値（テキスト）をブラウザに返却、画像は廃棄
   - ログ: Document Intelligenceの自動ログのみ、アプリケーションレベルでは画像を保存しない

2. **料金自動取得ボット (Python)**
   - 対象: 中国電力、JAでんき公式PDFのみ
   - robots.txt遵守、適切なUser-Agent設定
   - 取得日時、URL、ハッシュ値をログ記録
   - 個人情報なし（公開料金表のみ）

3. **Microsoft Entra ID統合**
   - 管理画面（料金マスター更新、ロールバック）のみ認証が必要
   - ユーザーはAADアカウント（社内メール）でログイン
   - 一般ユーザー向け公開画面は認証不要

4. **監査ログ**
   - 料金マスター更新: 誰が、いつ、何を、承認/却下したか
   - 取得元URL、ハッシュ値、変更前後の値
   - ログ保持期間: 12ヶ月以上（歴史トレーサビリティ）

### データプライバシー・データ保護評価 (DPIA)

#### 個人情報の処理活動

| 活動 | 個人情報の有無 | リスク | 対策 |
|------|----------------|--------|------|
| 使用量入力 | あり（使用量は「個人情報」） | 入力値保存 | ブラウザメモリのみ、永続化なし |
| 会社選択 | なし（選択肢リスト） | なし | - |
| 試算実行 | あり | ネットワーク送信 | Phase A では送信なし |
| PDF出力 | あり（数値のみ） | ファイル流出 | ダウンロード後はPCのみ保持、個人属性なし |
| 管理画面ログイン | あり（AAD） | 認証なし | Microsoft Entra ID |
| 料金マスター更新 | なし（数値のみ） | 誤った値の反映 | 人間承認、回帰テスト |

#### 最小データ原則

実装時に常に確認：

- [ ] この入力フィールドは本当に必要か？
- [ ] 計算結果に個人識別情報が含まれていないか？
- [ ] ブラウザメモリ以外にデータが保存されていないか？
- [ ] アクセスログ等に入力値が出力されていないか？

### テスト項目（セキュリティ）

Phase 4（UI実装時）以降の回帰テスト：

```bash
# ブラウザデータ確認
- localStorage が空であることを確認
- sessionStorage が空であることを確認
- IndexedDB が未使用であることを確認

# ネットワーク監視
- 使用量・契約会社の入力後、個人情報的データがサーバー送信されないこと
- HTTPS のみ接続（HSTS header）

# XSS テスト
- 計算結果表示にHTMLタグが含まれていないこと
- ユーザー入力（数値）で計算式が破損しないこと

# 個人情報混入確認
- PDF出力に氏名・住所・口座・ユーザーID が含まれないこと
- ブラウザコンソールに個人情報が出力されないこと（console.log削除）
```

### ドキュメント管理

元資料（Google Drive内）:

- **読み取り専用**: 変更・移動・削除・上書き厳禁
- **アクセス記録**: 監査ログなし（Google Driveの設定に依存）
- **バージョン管理**: Google DriveのバージョンHistory を活用

本プロジェクト内：

- **コード**: Git管理（.env は .gitignore）
- **料金マスター**: JSON（Git管理、更新は人間承認）
- **テスト用サンプルデータ**: 匿名化値のみ（実際の検針票使用なし）

---

**最終更新**: 2026-08-20
**関連文書**: DATA_GOVERNANCE.md, DEPLOY.md, SCRAPING_POLICY.md
